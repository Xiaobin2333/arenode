# 参与贡献

感谢你改进 Arenode。提交代码前请先搜索已有 Issue，较大的功能或接口变更建议先讨论设计和兼容范围。

## 本地开发

要求 Node.js 22.5+、PostgreSQL 17 和 Redis 7。复制 `.env.example` 为 `.env`，填写本地数据库与密钥后运行：

```bash
npm ci
npm test
npm run dev
```

## 提交要求

- 保持租户资源隔离，任何上游 ID 都不能直接暴露给客户浏览器。
- 新增写接口必须覆盖归属校验、字段白名单、错误回读和缓存失效测试。
- 不要提交 `.env`、截图产物、数据库、真实域名、IP、端口或 API 凭据。
- 用户界面文案使用面向客户的“CDN 服务”；CDNFly 和“上游”只用于管理员或开发文档。
- 所有测试应通过。真实上游测试必须使用专用资源及显式白名单，默认不得执行写操作。

## Pull Request

PR 请说明问题、实现方式、测试结果、兼容性影响和必要的部署步骤。涉及数据库结构时还应说明升级与回滚策略。

安全问题请按 [SECURITY.md](./SECURITY.md) 私密报告，不要创建公开 Issue。
