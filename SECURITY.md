# 安全策略

## 支持范围

安全修复只面向当前主分支。部署者应及时更新依赖和镜像，并在升级前备份 PostgreSQL。

## 报告漏洞

请不要在公开 Issue 中提交可利用细节、密钥、客户数据或真实域名。请通过仓库的 Security Advisory 私密报告，包含：

- 受影响的版本或提交；
- 最小复现步骤和影响范围；
- 已确认的前置条件；
- 可行的缓解或修复建议（如有）。

维护者确认收到后会先评估影响和临时缓解措施，再协调修复与披露时间。

## 部署者责任

- 生产环境必须由 HTTPS 反向代理对外提供服务，并正确设置 `APP_ORIGIN`。
- 不得启用 `MAIL_DEV_EXPOSE_CODE`；生产进程会忽略该配置。
- `.env` 权限应为 `0600`，不得提交到版本库。
- `SETTINGS_ENCRYPTION_KEY` 至少 32 个随机字符，并需离线备份。
- 公开注册默认关闭；配置 SMTP、邮箱验证和 Turnstile 后再按需开启。
- CDN API 凭据应使用权限最小化的普通用户账号，并定期轮换。
